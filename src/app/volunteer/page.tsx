import { useQuery } from 'convex/react'
import React from 'react'
import { api } from '../../../convex/_generated/api'

const Volunteer = () => {
  //const volunteer = useQuery(api.volunteer.GetVolunteersData);

  return (
    <div>Volunteer</div>
  )
}

export default Volunteer